# update_7 - 项目修改记录

---

## 2026-07-20

### [P0] 修复 `denoise_for_eval` 逆向扩散 Off-by-One 错误

**文件**: `DDPM_pytorch-main/train.py` — `denoise_for_eval()` 函数 (L330-L356)

**问题描述**:
逆向扩散循环中，所有数组索引均使用 `t_idx - 1`，导致模型接收的时间步与实际噪声水平差 1 位。

训练时加噪公式为 `x_t = sqrt(alphas_cumprod[t]) * x0 + sqrt(1-alphas_cumprod[t]) * noise`，模型学习映射 `(x_t, t) → noise`（t ∈ [0, 999]）。但推理循环第一步（t_idx=300）却将 `t_idx - 1 = 299` 传入模型，且使用 `alphas_cumprod[299]` 计算 pred_x0，与实际的 `alphas_cumprod[300]` 噪声水平不匹配。

**修改内容**（6 处 `t_idx - 1` → `t_idx`）:

| 行 | 修改前 | 修改后 |
|----|--------|--------|
| 333 | `torch.full(..., t_idx - 1, ...)` | `torch.full(..., t_idx, ...)` |
| 337 | `alphas[t_idx - 1]` | `alphas[t_idx]` |
| 338 | `alphas_cumprod[t_idx - 1]` | `alphas_cumprod[t_idx]` |
| 339 | `alphas_cumprod_prev[t_idx - 1]` | `alphas_cumprod_prev[t_idx]` |
| 340 | `betas[t_idx - 1]` | `betas[t_idx]` |
| 351 | `posterior_variance[t_idx - 1]` | `posterior_variance[t_idx]` |

**影响范围**: 所有依赖 `denoise_for_eval` 的评估（`evaluate_checkpoint`、`visualize_denoising`、SNR 监控、`eval_multi_t.py`、`evaluate_errors.py`），修复后会随函数定义自动生效。

**备注**: 此前所有基于该函数的评估指标均不准确，需重新训练和评估。

---

### [P1] 修复训练/推理 Clamp 范围不一致

**文件**: `DDPM_pytorch-main/train.py` — `denoise_for_eval()` 函数 (L343)

**问题描述**:
训练和验证阶段 `pred_clean` 的 clamp 范围为 `[-1, 1]`，而推理函数 `denoise_for_eval` 中 `pred_x0` 的 clamp 范围为 `[-1.2, 1.2]`。这种不一致会导致训练-推理分布偏移：模型在训练时从未见过超出 [-1, 1] 的输出，但推理时却允许 ±1.2 的值通过。

**修改内容**:

| 行 | 修改前 | 修改后 |
|----|--------|--------|
| 343 | `torch.clamp(pred_x0, -1.2, 1.2)` | `torch.clamp(pred_x0, -1, 1)` |

**影响范围**: `denoise_for_eval` 被 `evaluate_checkpoint`、`visualize_denoising`、SNR 评估、`eval_multi_t.py`、`evaluate_errors.py` 调用，统一后训练与推理行为一致。

---

### [Doc] 更新 README.md

**文件**: `DDPM_pytorch-main/README.md`

**修改内容**:
- 修复开头多余的 "111" 字符
- 更新文件结构树，反映当前实际文件（新增 `eval_multi_t.py`、`evaluate_errors.py`、`baseline_methods.py`、`run_ablation.py`、`tests/`）
- 移除已删除文件（`check_data_leakage.py`、`check_structure.py`）
- 新增数据集划分设计说明（无泄露验证）
- 新增运行测试说明
- 更新评估体系描述

---

### [P1] 修复 `reverse_diffusion_process.py` 两处问题

**文件**: `DDPM_pytorch-main/reverse_diffusion_process.py`

**问题1** — Clamp 不一致 (L104):
| 修改前 | 修改后 |
|--------|--------|
| `torch.clamp(pred_x0, -1.2, 1.2)` | `torch.clamp(pred_x0, -1, 1)` |

**问题2** — 模型架构不兼容 (L273-281):
- 旧代码使用 `DenoiseUNet`（`models.py` 中的 `ImprovedDenoiseUNet1D`）
- 但 `train.py` 训练使用的是 `SimpleUNet1D_Classic`，两者架构不同，导致无法加载训练好的权重
- 修复：1D 模式下改用 `SimpleUNet1D_Classic`（从 `train.py` 导入），2D 模式保留 `DenoiseUNet` 作为兼容

### [Note] `generate_result.py` 生成的是假数据

**文件**: `DDPM_pytorch-main/generate_result.py`

该脚本不使用 DDPM 模型，而是直接对干净信号加随机噪声和扰动来生成 `result.npy`。现有的 `result.npy`（16MB）可能是由此脚本生成的占位数据，不代表真实的模型去噪效果。评估时应使用 `evaluate_errors.py` 直接从模型计算。

